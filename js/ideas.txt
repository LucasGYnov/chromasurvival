checkEnemyCollision(enemies) {
    for (let i = 0; i < enemies.length; i++) {
        const enemy = enemies[i];
        if (
            collisionDetection({
                object1: this.hitbox,
                object2: enemy.hitbox,
            })
        ) {
            if (this.velocity.x > 0) {
                this.velocity.x = 0;

                const offset = this.hitbox.position.x - this.position.x + this.hitbox.width;

                this.position.x = enemy.hitbox.position.x - offset - 0.01;
                break;
            }
            if (this.velocity.x < 0) {
                this.velocity.x = 0;

                const offset = this.hitbox.position.x - this.position.x;

                this.position.x = enemy.hitbox.position.x + enemy.hitbox.width - offset + 0.01;
                break;
            }
        }
    }
}

marche ici comme block mouvement + collision avec joueur